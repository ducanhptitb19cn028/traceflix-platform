server.port=8080
spring.application.name=${SERVICE_NAME:mesh-service}
# Comma-separated full downstream URLs (e.g. http://catalog-service:8080/api/call).
# Empty => leaf node. Wired per deployment via the DOWNSTREAM_URLS env var.
downstream.urls=${DOWNSTREAM_URLS:}
