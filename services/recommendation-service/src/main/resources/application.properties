server.port=8080
spring.application.name=${SERVICE_NAME:recommendation-service}
# downstream: catalog (override with CATALOG_SERVICE_URL)
catalog-service.url=${CATALOG_SERVICE_URL:http://catalog-service:8080}
