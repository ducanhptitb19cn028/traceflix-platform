spring.application.name=${SERVICE_NAME:user-service}
spring.jpa.hibernate.ddl-auto=none
# downstreams (override with AUTH_SERVICE_URL / RECOMMENDATION_SERVICE_URL)
auth-service.url=${AUTH_SERVICE_URL:http://auth-service:8080}
recommendation-service.url=${RECOMMENDATION_SERVICE_URL:http://recommendation-service:8080/api/recommendations}
